{
  "name": "eduadapt",
  "private": true,
  "description": "Run FastAPI backend + Next.js UI together",
  "scripts": {
    "dev": "concurrently -n api,web -c blue,magenta \"npm run dev:api\" \"npm run dev:web\"",
    "dev:api": "python -m uvicorn main:app --reload --host 127.0.0.1 --port 8000",
    "dev:web": "npm run dev --prefix web",
    "install:web": "npm install --prefix web"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  }
}
